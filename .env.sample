## Copy this file to ".env" and fill values for local dev.

## Postgres basics (used by db container)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=YOUTUBE_ANALYSIS_DEV

## Split DB_* variables used by services and migrations
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=YOUTUBE_ANALYSIS_DEV
DB_SSLMODE=disable

## Migrations directory (optional at runtime)
MIGRATIONS_DIR=services/authority-service/internal/driver/datastore/migrations

## authority-service
GRPC_ADDR=:8080

## Identity Platform / OIDC (required by authority-service)
# Example (Firebase):
#   OIDC_ISSUER=https://securetoken.google.com/<PROJECT_ID>
#   OIDC_AUDIENCE=<PROJECT_ID>
FIREBASE_API_KEY=
OIDC_ISSUER=
OIDC_AUDIENCE=
